import { DashboardTabClientContent } from "../../../../components/DashboardTab";

/**
 * Global Settings Page
 * URL: /global/settings
 * Shows global configuration: theme, features, businesses management, etc.
 */
export default function GlobalSettingsPage() {
  return <DashboardTabClientContent tab="settings" />;
}
