import { DashboardTabClientContent } from "@/components/DashboardTab";

/**
 * Global Brain/Memory Page
 * URL: /global/brain
 * Shows memory and knowledge base (globally shared)
 */
export default function GlobalBrainPage() {
  return <DashboardTabClientContent tab="brain" />;
}
