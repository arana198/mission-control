# Mission Control Docker Environment Configuration
# Copy this file to .env when deploying with Docker Compose

# Required: Convex cloud project URL
# Find this in your Convex dashboard under "Settings" or from `npx convex dev` output
# Example: https://my-project-abc123.convex.cloud
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Optional: Public app URL for agent provisioning and webhooks
# Defaults to http://localhost:3000 in Docker
# Set this to your public domain when deploying
NEXT_PUBLIC_APP_URL=http://localhost:3000
